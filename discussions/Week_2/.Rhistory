/Library/Frameworks/R.framework/Resources/bin/R
file.path(R.home(), "bin", "R")
Sys.getenv("RSTUDIO_PANDOC")
# Don't edit anything within this code block (lines 13-18). Code blocks are book ended with ```. Anything within these lines are treated as code by the computer. Anything outside of code blocks is treated like formatted text.
knitr::opts_chunk$set(echo = TRUE)
knitr::opts_chunk$set(eval = TRUE)
knitr::opts_chunk$set(error = TRUE)
firstname <- "Zakir"
firstname <- "Zakir"   # example: "Liam". Here, we're creating an object named 'firstname' where we can store a first name like "Liam".
lastname <- "Alibhai"  # example: "Mueller", Similarly, we're creating an object named 'lastname' where we can store a last name like "Mueller".
username <- "zalibhai" # example: "lomueller" As above, we are creating an object named username which will store information, in this case, your UCSD username (your ucsd email with out the @ucsd.edu part)
firstname <- "Zakir"   # example: "Liam". Here, we're creating an object named 'firstname' where we can store a first name like "Liam".
lastname <- "Alibhai"  # example: "Mueller", Similarly, we're creating an object named 'lastname' where we can store a last name like "Mueller".
username <- "zalibhai" # example: "lomueller" As above, we are creating an object named username which will store information, in this case, your UCSD username (your ucsd email with out the @ucsd.edu part)
# save your output in variable name "q1". (for example: q1 <- "your answer")
q1 <- "I have programmed mainly in Python and JavaScript."
# Now try and print the object q1 by typing a function on the line after this note. A function is a pre-made set of instructions that you can command the computer to do on an object. For example: pasting the function "abs(-13)" in your console and pressing enter will give the absolute value of -13. Hint: print() is a function.
print(q1)
# save your output in variable name "q1". (for example: q1 <- "your answer")
q1 <- "I have programmed mainly in Python and JavaScript."
# Now try and print the object q1 by typing a function on the line after this note. A function is a pre-made set of instructions that you can command the computer to do on an object. For example: pasting the function "abs(-13)" in your console and pressing enter will give the absolute value of -13. Hint: print() is a function.
print(q1)
cat(q1)
q1
# save your output in variable name "q1". (for example: q1 <- "your answer")
q1 <- "I have programmed mainly in Python and JavaScript."
# Now try and print the object q1 by typing a function on the line after this note. A function is a pre-made set of instructions that you can command the computer to do on an object. For example: pasting the function "abs(-13)" in your console and pressing enter will give the absolute value of -13. Hint: print() is a function.
print(q1)
q1
cat(q1)
q1
q2 <- "I'm excited to learn about machine learning, because I know that it can have AI applications."
# Please delete the brackets after you write your answer within the quotes. On the line below please print out your response.
print(q2)
install.packages("rmarkdown", dep = TRUE)
install.packages("tinytex")
install.packages("tidyr")
install.packages("tidyverse")
# Don't edit anything within this code block (lines 13-18). Code blocks are book ended with ```. Anything within these lines are treated as code by the computer. Anything outside of code blocks is treated like formatted text.
knitr::opts_chunk$set(echo = TRUE)
knitr::opts_chunk$set(eval = TRUE)
knitr::opts_chunk$set(error = TRUE)
c(2,7,4,5,2,12)
Y
print(Y)
Y<- c(2,7,4,5,2,12)
Y
print(Y)
setwd("~/RStudio")
setwd("~/CSE_Notes/BILD_5")
setwd("~/CSE_Notes/BILD_5/discussions")
setwd("~/CSE_Notes/BILD_5/discussions/Week_2")
ls
---
title: "Section Assignment 2"
# Don't edit anything within this code block (lines 13-18). Code blocks are book ended with ```. Anything within these lines are treated as code by the computer. Anything outside of code blocks is treated like formatted text.
knitr::opts_chunk$set(echo = TRUE)
knitr::opts_chunk$set(eval = TRUE)
knitr::opts_chunk$set(error = TRUE)
firstname <- "Zakir"   # example:firstname <- "Liam"
lastname <- "Alibhai"  # example: lastname <-"Mueller"
username <- "zalibhai"  #example: username <- "lomueller"   Do not include the "@ucsd.edu" part!
X<- c(1,3,5,6,7,9)
X
Y<- c(2,7,4,5,2,12)
Y
### This chunk of code is for grading purposes.
### Don't change/delete this chunk of code.
q1 <- X
q2 <- Y
Z <- c(X,Y)
Z
### This chunk of code is for grading purposes.
### Don't change/delete this chunk of code.
q3 <- Z
meanZ<- mean(Z)
meanZ
### This chunk of code is for grading purposes.
### Don't change/delete this chunk of code.
q4 <- meanZ
newZ<- Z<7
newZ
### This chunk of code is for grading purposes.
### Don't change/delete this chunk of code.
q5 <- newZ
lengthX<- length(newZ)
lengthX
### This chunk of code is for grading purposes.
### Don't change/delete this chunk of code.
q6 <- lengthX
DataWeek2 <- read.csv(file = 'Child_Heart_Surgery.csv')
head(DataWeek2)
### This chunk of code is for grading purposes.
### Don't change/delete this chunk of code.
q7 <- DataWeek2
SurvivalRate <-
SurvivalRate
### This chunk of code is for grading purposes.
### Don't change/delete this chunk of code.
q8 <- SurvivalRate
DataWeek2new <-
head(DataWeek2new)
### This chunk of code is for grading purposes.
### Don't change/delete this chunk of code.
q9 <- DataWeek2new
q10 <- "[Your answer here]"
q11 <- "[Your answer here]"
q12 <- "[Your answer here]"
newZ<- Z[Z<7]
newZ
lengthX<- length(newZ)
lengthX
DataWeek2 <- read.csv(file = 'Child_Heart_Surgery.csv')
head(DataWeek2)
survivors<-sum(DataWeek2[,3])
operations<-sum(DataWeek2[,2])
SurvivalRate <- survivors/operations
SurvivalRate
DataWeek2 <- read.csv(file = 'Child_Heart_Surgery.csv')
head(DataWeek2)
SurvivalRate <- apply(X = DataWeek2,MARGIN = 1,FUN = DataWeek2[,3]/DataWeek2[,2])
SurvivalRate
test1 <- apply(X = DataWeek2,MARGIN = 1,FUN = SUM)
SurvivalRate <-
SurvivalRate
test1 <- apply(X = DataWeek2,MARGIN = 1,FUN = sum)
SurvivalRate <-
SurvivalRate
test1 <- apply(X = DataWeek2,MARGIN = 1,FUN = sum)
#SurvivalRate <-
#SurvivalRate
test1
test1 <- apply(X = DataWeek2,MARGIN = 2,FUN = sum)
#SurvivalRate <-
#SurvivalRate
test1
View(DataWeek2)
#survivors<-sum(DataWeek2[,3])
#operations<-sum(DataWeek2[,2])
#rate<-survivors/operations
SurvivalRate <- apply(X,MARGIN=1,FUN = (DataWeek2[,3])/(DataWeek2[,2]))
SurvivalRate
#survivors<-sum(DataWeek2[,3])
#operations<-sum(DataWeek2[,2])
#rate<-survivors/operations
SurvivalRate <- apply(X,MARGIN=1,FUN = divide((DataWeek2[,3])/(DataWeek2[,2])))
SurvivalRate
#survivors<-sum(DataWeek2[,3])
#operations<-sum(DataWeek2[,2])
#rate<-survivors/operations
SurvivalRate <- apply(X,MARGIN=1,FUN = function((DataWeek2[,3])/(DataWeek2[,2])))
SurvivalRate
#survivors<-DataWeek2[,3])
#operations<-DataWeek2[,2])
#rate<-survivors/operations
SurvivalRate <- apply(X,MARGIN=1,FUN = /)
SurvivalRate
#survivors<-DataWeek2[,3])
#operations<-DataWeek2[,2])
#rate<-survivors/operations
SurvivalRate <- apply(X,MARGIN=1,FUN = DataWeek2[,3]/DataWeek2[,2])
SurvivalRate
#survivors<-DataWeek2[,3])
#operations<-DataWeek2[,2])
#rate<-survivors/operations
SurvivalRate <- apply(X,MARGIN=1,FUN = /)
SurvivalRate
#survivors<-DataWeek2[,3])
#operations<-DataWeek2[,2])
#rate<-survivors/operations
SurvivalRate <- apply(X = DataWeek2,MARGIN=1,FUN = "/")
SurvivalRate
#survivors<-DataWeek2[,3])
#operations<-DataWeek2[,2])
#rate<-survivors/operations
SurvivalRate <- apply(X = DataWeek2,MARGIN=1,FUN = "DataWeek2[,3]/DataWeek2[,2]")
SurvivalRate
#survivors<-DataWeek2[,3])
#operations<-DataWeek2[,2])
#rate<-survivors/operations
SurvivalRate <- apply(X = DataWeek2,MARGIN=1,FUN = "/")
SurvivalRate
survivors<-DataWeek2[,3]
operations<-DataWeek2[,2]
NewDataFrame<-c(survivors,operations)
#rate<-survivors/operations
SurvivalRate <- apply(X = NewDataFrame,MARGIN=1,FUN = "/")
SurvivalRate
survivors<-DataWeek2[,3]
operations<-DataWeek2[,2]
NewDataFrame<-c(survivors,operations)
#rate<-survivors/operations
SurvivalRate <- survivors/operations
SurvivalRate
#survivors<-DataWeek2[,3]
#operations<-DataWeek2[,2]
#rate<-survivors/operations
SurvivalRate <- DataWeek2[,3]/DataWeek2[,2]
SurvivalRate
### This chunk of code is for grading purposes.
### Don't change/delete this chunk of code.
q7 <- DataWeek2
DataWeek2 <- read.csv(file = 'Child_Heart_Surgery.csv')
head(DataWeek2)
### This chunk of code is for grading purposes.
### Don't change/delete this chunk of code.
q7 <- DataWeek2
#survivors<-DataWeek2[,3]
#operations<-DataWeek2[,2]
#rate<-survivors/operations
SurvivalRate <- DataWeek2[,3]/DataWeek2[,2]
SurvivalRate
# Don't edit anything within this code block (lines 13-18). Code blocks are book ended with ```. Anything within these lines are treated as code by the computer. Anything outside of code blocks is treated like formatted text.
knitr::opts_chunk$set(echo = TRUE)
knitr::opts_chunk$set(eval = TRUE)
knitr::opts_chunk$set(error = TRUE)
firstname <- "Zakir"   # example:firstname <- "Liam"
lastname <- "Alibhai"  # example: lastname <-"Mueller"
username <- "zalibhai"  #example: username <- "lomueller"   Do not include the "@ucsd.edu" part!
X<- c(1,3,5,6,7,9)
X
Y<- c(2,7,4,5,2,12)
Y
### This chunk of code is for grading purposes.
### Don't change/delete this chunk of code.
q1 <- X
q2 <- Y
Z <- c(X,Y)
Z
### This chunk of code is for grading purposes.
### Don't change/delete this chunk of code.
q3 <- Z
meanZ<- mean(Z)
meanZ
### This chunk of code is for grading purposes.
### Don't change/delete this chunk of code.
q4 <- meanZ
newZ<- Z[Z<7]
newZ
### This chunk of code is for grading purposes.
### Don't change/delete this chunk of code.
q5 <- newZ
lengthX<- length(newZ)
lengthX
### This chunk of code is for grading purposes.
### Don't change/delete this chunk of code.
q6 <- lengthX
DataWeek2 <- read.csv(file = 'Child_Heart_Surgery.csv')
head(DataWeek2)
### This chunk of code is for grading purposes.
### Don't change/delete this chunk of code.
q7 <- DataWeek2
#survivors<-DataWeek2[,3]
#operations<-DataWeek2[,2]
#rate<-survivors/operations
SurvivalRate <- DataWeek2[,3]/DataWeek2[,2]
SurvivalRate
### This chunk of code is for grading purposes.
### Don't change/delete this chunk of code.
q8 <- SurvivalRate
DataWeek2new <- cbind(DataWeek2,SurvivalRate)
head(DataWeek2new)
### This chunk of code is for grading purposes.
### Don't change/delete this chunk of code.
q9 <- DataWeek2new
# Saving it to CSV
write.csv(x = DataWeek2new,file = 'New_CSV_File_Rate')
q10 <- "The goal of science is to understand, describe, and predict phenomenon in the universe."
q11 <- "This would be deductive reasoning, because you are testing a hypothesis and falsifying a statement (the presence of a side effect)."
q12 <- "The Human Genome Project is an example of a scientific inductive model, because it was created from a series of experiments and observations testing the genomes of subjects, and was constantly updated with new information. It has changed over time to more accurately and fully represent the genetic sequences and DNA of humans. Citation: https://www.genome.gov/human-genome-project"
# Saving it to CSV
write.csv(x = DataWeek2new,file = 'New_CSV_File_Rate')
# Saving it to CSV
write.csv(x = DataWeek2new,file = 'New_CSV_File_Rate.csv')
# Saving it to CSV
write.csv(x = DataWeek2new,file = 'New_CSV_Rate.csv')
# Saving it to CSV
#write.csv(x = DataWeek2new,file = 'New_CSV_Rate.csv')
